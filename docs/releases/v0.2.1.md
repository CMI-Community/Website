# Release v0.2.1

- 状态：Released
- 日期：2026-08-11
- Tag：`v0.2.1`
- Commit：发布后补充
- GitHub Release：发布后补充
- 关联 Issue/PR：[Issue #16](https://github.com/CMI-Community/Website/issues/16) / [PR #17](https://github.com/CMI-Community/Website/pull/17)
- Runtime impact：production
- 人工批准：内容负责人于 2026-08-11 明确要求检查误发布并立即带上新小红书链接发布三屏新版本

## Incident and Root Cause

- `cmi.community` 被旧目录 `CMI 海报工程/poster-wall` 中的 `cmi-community-site` Worker 误发布覆盖。
- 根因是发布前没有先确认新官网仓库、正确 production Worker 和三屏首页验收合同。
- 发现后创建 Issue #16，并从本仓库 `main` 使用 `env.production` 恢复 `cmi-community-platform`，没有迁移或删除 D1/R2 数据。

## Changes

- 保持 Community Hero、Photo Museum、Event Museum 三屏正式首页以及独立 `/archive/posters`。
- 将共享小红书入口更新为 `https://xhslink.cn/m/3C1MCJgpWq6`，同时覆盖首页与海报档案。
- 增加固定目标单元测试，防止共享配置退回旧链接。

## Data and Migration

- 无 D1 migration，无 R2 写入或删除，无用户数据结构变更。
- 继续读取 production R2 `cmi-community-public-assets/photo-museum/v2/` 与 `poster-wall/v1/`。

## Configuration

- Worker：`cmi-community-platform`
- D1：`cmi-community-platform`（未迁移）
- R2：`cmi-community-public-assets`（未修改）
- 生产域名：`cmi.community`、`www.cmi.community`

## Verification

- `VERIFIED`：恢复 Worker `6cb12718-4630-43b1-8988-7598e6043f8d` 后，根路径 200 且包含 `home`、`photo-museum`、`event-museum` 三个区域。
- `VERIFIED`：`/api/v1/health` 返回 `cmi-community-platform / production`；`/archive/posters`、公开反馈 API、代表性 Photo Museum v2 缩略图与全图均为 200。
- `VERIFIED`：`www` 以 308 保留 path/query 转向裸域。
- `VERIFIED`：v0.2.1 `npm run check` 通过 lint、公开边界、trace、typecheck、25 tests、D1 migration smoke、SSR build 与 staging dry-run；生产依赖审计为 0 个漏洞。
- `VERIFIED`：本地 Playwright 桌面与 390px 共 7 passed、1 skipped；三屏首页、视口、健康和身份/发布角色回归通过。
- `PENDING`：production dry-run、Worker 发布、线上桌面与 390px 验收。

## Rollback

- 若小红书补丁造成异常，回滚到本次恢复 Worker `6cb12718-4630-43b1-8988-7598e6043f8d`；该版本保持 v0.2.0 三屏官网，仅使用旧小红书直链。
- 回滚不删除 D1、R2 或事故历史；回滚后重新验证三屏、照片资产、海报档案、反馈 API、健康接口与 `www`。
